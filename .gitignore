# Data IS included in this submission (both cleaned and original raw sets).
# Only caches and redundant archives are excluded.

# ── Python caches ──
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ── redundant archives / scratch ──
*.zip
star_xy.csv

# ── OS / editor junk ──
.DS_Store
*.swp
